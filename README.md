# Kara
a time-tracking app, with heavy inspiration from **Toggl Track**.

Currently, only three screens are implemented (with use of MVP):
- Login Screen
- Register Screen
- Home Screen

The app also uses a List instead of a database. I'll also update this README whenever I can. But yeah, stay tuned for the final product :D